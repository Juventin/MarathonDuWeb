import React, {Component} from 'react';
import '../App.css';
import styled from "styled-components";

const Box = styled.div`

`;

export default class MariadbMap extends Component {

    render() {
        return (
            <Box className="middle">
                <p>
                  Slt la mystery machine !
                </p>
            </Box>
        );
    };
};